management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream
  endpoint:
    hystrix:
      stream:
        enabled: true
hystrix:
  dashboard:
    proxy-stream-allow-list: 127.0.0.1,192.168.56.1